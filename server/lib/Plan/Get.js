const Functions = require('../../util/Functions')
const db = require('../../db')

/**
 * Get Plans
 * @param req
 * @param res
 */
module.exports = async(req, res) => {
    const { data } = req.body

    //send back possible options
    res.json({})
}
