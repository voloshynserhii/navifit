import { Text, StyleSheet } from 'react-native';
import AppUserView from '@/components/AppUserView';

export default function WorkoutScreen() {
  return (
    <AppUserView title='Workout'>
      <Text></Text>
    </AppUserView>
  );
}

const styles = StyleSheet.create({

});